# Backend Guarantees and Capability Matrix

This document defines the expected semantics and tested capability differences by backend.

There is no blanket delivery guarantee shared by every backend. Handlers should be idempotent because durable backends may redeliver, while ephemeral backends may lose work at failure boundaries described below.

## Semantics Baseline (All Backends)

- Acceptance and delivery durability are backend-specific.
- Duplicate processing: possible; handlers must be idempotent
- Ordering: not guaranteed unless explicitly documented for a backend/runtime path
- A successful dispatch means the backend-specific acceptance boundary was crossed; it does not imply handler success.
- Unsupported or unproven guarantees must remain explicit rather than being inferred from another backend's tests.

## Capability Matrix (Integration Fixture-Aligned)

The table below reflects capability gates used by `integration/all/integration_scenarios_test.go`. A `Yes` means that fixture currently runs the linked scenario; it is evidence under that fixture's conditions, not by itself a production guarantee across crashes, producer/worker separation, or multiple processes.

| Backend | Backoff | Restart Scenario | Delayed/Retry Restart Scenario | Poison Retry | Dispatch Context Cancel | Fixture No-Dupes | Ordering Scenario | Broker Fault Scenarios | Shutdown During Delay/Retry |
| --- | :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| `redis` | No | Yes | Yes | No | No | Yes | Yes | Yes | Yes |
| `mysql` | Yes | Yes | Yes | Yes | Yes | Yes | No | No | Yes |
| `postgres` | Yes | Yes | Yes | Yes | Yes | Yes | No | No | Yes |
| `sqlite` | Yes | Yes* | Yes* | Yes | Yes | Yes | Yes* | No | Yes |
| `nats` | Yes | No | No | Yes | No | No | No | No | No |
| `sqs` | Yes | Yes | No | Yes | Yes | Yes | No | No | No |
| `rabbitmq` | Yes | Yes | Yes | Yes | No | Yes | No | No | Yes |

### Scenario References (Proof Links)

These are the primary shared-scenario proofs for the matrix above.

| Capability / Guarantee | Proving Scenario(s) / Test(s) |
| --- | --- |
| Backoff support / rejection behavior | `scenario_config_option_fuzz`; Redis explicit unsupported path in `TestRedisIntegration_BackoffUnsupported` (`integration/all/integration_scenarios_test.go`) |
| Delay/retry "not before" timing windows | `scenario_retry_delay_timing_windows` parent with `scenario_delay_not_before_window` and `scenario_retry_backoff_not_before_window` (capability-gated) |
| Restart recovery | `scenario_worker_restart_recovery` |
| Delayed/retry restart durability | `scenario_worker_restart_delay_recovery`; `scenario_shutdown_during_delay_retry` |
| Poison retry semantics | `scenario_poison_message_max_retry` |
| Dispatch context cancellation | `scenario_dispatch_context_cancellation` parent with `scenario_dispatch_context_precanceled`, `scenario_dispatch_context_deadline_exceeded`, and `scenario_dispatch_context_followup_health` |
| No-duplicate processing under fixture conditions | `scenario_multi_worker_contention`; `scenario_duplicate_delivery_idempotency`. Public logical `Job.UniqueFor` behavior is separately exercised through `TestIntegrationQueue_AllBackends` in `integration/all/runtime_integration_test.go`. |
| Ordering contract (suite-level capability) | `scenario_ordering_contract` parent with `scenario_ordering_single_worker_fifo`; concurrent non-guarantee covered by `scenario_ordering_multi_worker_best_effort`; reordering behavior probed by `scenario_ordering_delayed_immediate_mix` and `scenario_ordering_retry_reorder_allowed` |
| Broker fault handling | `scenario_dispatch_during_broker_fault`; `scenario_consume_after_broker_recovery`; Redis lost-ack recovery in `TestIntegrationChaos_RedisBrokerDisconnectRedelivery` |
| Shutdown during delay/retry workloads | `scenario_shutdown_during_delay_retry` |
| Pause/Resume capability behavior | `TestObservabilityIntegration_PauseResumeSupport_AllBackends` (`integration/root/observability_integration_test.go`) |
| Native stats capability behavior | `TestObservabilityIntegration_AllBackends` (`integration/root/observability_integration_test.go`) |
| Queue/workflow chain/batch integration baseline | `TestIntegrationQueue_AllBackends` (`integration/all/runtime_integration_test.go`); `TestIntegrationBus_AllBackends` (`integration/bus/integration_test.go`) |
| Workflow failure callback semantics (catch/finally + terminal state) | `TestIntegrationBus_AllBackends` -> `workflow_chain_failure_callbacks` and `workflow_batch_failure_callbacks` (`integration/bus/integration_test.go`) |
| Workflow duplicate callback suppression (SQL runtime/store path) | `TestSQLStore_RuntimeChainFinallyDuplicateCallbackSuppressed`; `TestSQLStore_RuntimeChainCatchAndFinallyDuplicateCallbacksSuppressed`; `TestSQLStore_RuntimeChainFinallyCallbackReplayAfterDispatchFaultSuppressed`; existing batch duplicate callback suppression tests (`integration/bus/callback_sql_integration_test.go`) |
| Workflow dispatch failure state consistency (SQL runtime/store path) | `TestSQLStore_RuntimeChainInitialDispatchFailureStateConsistent`; `TestSQLStore_RuntimeBatchPartialDispatchFailureStateConsistent` (`integration/bus/dispatch_failure_sql_integration_test.go`) |
| Externally managed queue-schema readiness | `TestDatabaseManagedSchemaValidationRejectsIncompleteBackends`, `TestDatabaseManagedSchemaRejectsNonBaseRelations`, and `TestDatabaseManagedSchemaPreflightAndStartupRetry` (`driver/sqlqueuecore/queue_database_failure_test.go`) cover missing operational tables, view-backed relations, and runtime-used columns without DDL. `TestDatabaseIntegration_SQLite/sqlite_managed_schema_fails_closed_then_recovers_after_provisioning` proves an empty schema fails both readiness and startup without mutation, then the same runtime succeeds after external provisioning. The `sqlite_managed_schema_dispatch_and_process`, `mysql_managed_schema_dispatch_and_process`, and `postgres_managed_schema_dispatch_and_process` scenarios prove canonical preprovisioned schemas support readiness, uniqueness, dispatch, and consumption on every SQL dialect (`integration/root/database_integration_test.go`). |
| Atomic workflow-store transitions across SQL dialects | `TestWorkflowStoreIntegration_SQLite`; `TestWorkflowStoreIntegration_MySQL`; `TestWorkflowStoreIntegration_MySQLAutoMigratesMissingReceiptAtLegacyWidths`; `TestWorkflowStoreIntegration_MySQLManagedWideKeys`; `TestWorkflowStoreIntegration_MySQLRejectsNonVARBINARYKeys`; `TestWorkflowStoreIntegration_Postgres` (`integration/root/workflow_store_integration_test.go`), plus shared memory/SQLite concurrency contracts (`internal/workflow/store_contract_test.go`). The MySQL upgrade fixture drops only the receipt table beside widened legacy state, verifies ordinary startup derives 512-byte receipt identities, and exercises identities above the fresh-schema defaults. The managed fixture separately covers a complete pre-existing wider schema. |
| Current-generation retention after a committed workflow transition | `TestDeliveryApplicationStateCommittedSignal` (`busruntime/delivery_settlement_test.go`), `TestDatabasePendingRecoveryTokenPreservesPendingRecovery` (`driver/sqlqueuecore/queue_database_impl_test.go`), and `TestChainPostTransitionFailureMarksCurrentGenerationForRecovery` (`internal/workflow/workflow_mutation_retry_test.go`) prove the direct signal, SQL token selection, and post-transition workflow handoff. The end-to-end receipt recovery row below covers all three SQL dialects. |
| Versioned transition-receipt interpretation | `TestTransitionReceiptUnknownVersionsFailClosed` and `TestUnknownTransitionReceiptVersionsBlockRecoveredApplicationExecution` (`internal/workflow`) prove unknown `receipt_version` or `event_schema_version` values return an uncommitted recovery error: the worker neither acknowledges the row, runs application code, marks application state committed, nor publishes reconstructed facts. The observer event schema is shared across queue, worker, and workflow layers and is versioned independently from the workflow-envelope protocol. The real SQL recovery scenarios below exercise the supported version columns through each dialect's physical schema and read/write path. |
| Receipt-backed workflow facts after definite SQL finalization failure | `TestDatabaseIntegration_SQLite/sqlite_workflow_success_facts_recover_after_finalization_failure` covers a terminal chain, a predecessor whose successor already completed, a batch member, and one-member aggregate completion on a real stale SQLite row. `TestRecoveredTransitionReceiptLogicalValidationSeparatesPhysicalOwnership` separates valid logical receipt proof from the exact physical fact owner. `TestChainSuccessorRejectionRecoversWithoutPredecessorReplay` proves exact-owner recovery repeatedly re-dispatches the immediate successor after definite enqueue rejection without re-running the predecessor. `TestChainRecoveryWithoutExactReceiptOwnershipPreservesOnlyLiveContinuation` covers a missing receipt, a decorated store without receipt capability, and different/legacy generation provenance. `TestChainSuccessRecoveryAllowsDifferentPhysicalDeliveryIdentity` adds valid duplicate chain deliveries with a different attempt or physical `JobID`: only the live immediate successor is dispatched, while the predecessor handler, facts, and callbacks are not replayed. Progressed or terminal state is a no-op, and a rejected replacement enqueue remains uncommitted for retry. `TestChainSuccessRecoveryRejectsInvalidReceiptShapeBeforeLiveness` proves a supported success receipt must reject cancellation and make completion exactly match final-node position before either facts or continuation dispatch. `TestBatchRecoverySettlesNonFactOwnersWithoutFacts` proves successful batch duplicates settle silently and failed duplicates return a generic permanent cause across different attempts, different recovered generations, and legacy provenance, without handlers, callbacks, or facts. Dispatch remains intentionally at-least-once because recovery cannot distinguish a missing successor from one queued but not yet progressed. `sqlite_terminal_batch_completion_recovers_from_completing_member` proves a two-member batch reconstructs `BatchCompleted` only for the recovered member whose receipt owns the aggregate transition. `sqlite_failed_batch_recovery_archives_without_reexecution` proves a failed receipt reaches SQL's `dead` archive without fabricating the unpersisted application cause or executing either handler again. `sqlite_later_workflow_attempt_wins_then_recovers_without_reexecution` proves that application retry does not inherit the earlier generation. `mysql_workflow_receipt_recovery` and `postgres_workflow_receipt_recovery` prove terminal-chain state/fact recovery through each server dialect (`integration/root/database_integration_test.go`). This does not cover other settlement owners, callbacks, durable continuation intent, or no-redelivery publication. |
| Receipt-backed terminal chain failure | `TestChainCommittedFailureRecoveryPreservesOneApplicationOccurrence` proves exact, different, and legacy recovered-generation provenance all return the first persisted permanent chain cause without another handler, Catch/Finally callback, or logical failure fact. `TestChainFailureRecoveryAllowsDifferentPhysicalDeliveryIdentity` extends the same archive classification to valid duplicates with a different attempt or physical `JobID`. `TestChainFailureRecoveryRejectsInvalidReceiptIdentity`, the transition-claim/incarnation contracts, and `TestTransitionReceiptAmbiguousCommitReadback/chain_failure` cover malformed logical identity, terminal shape, and commit readback; `TestChainFailureRecoveryWithoutPersistedCauseUsesTerminalDiagnostic` pins the empty-cause permanent diagnostic. `TestChainLegacyFailureRecoveryRetainsCurrentFailureClassification` records the weaker receipt-absent fallback, which reruns application code once for physical classification while suppressing duplicate facts/callbacks. `TestStoreContract_FailChainPreservesReceiptBackedCause` covers memory and SQLite first-cause preservation, and `TestSQLStoreChainFailureReceiptRollsBackParent` proves receipt-insert failure cannot commit parent failure without provenance. `TestDatabaseIntegration_SQLite/sqlite_failed_chain_recovery_archives_without_reexecution` then forces repeated real archive failure, preserves receipt owner, attempt zero, and cause, avoids duplicate application/workflow occurrences, and eventually reaches `dead` at attempt one with that persisted cause. Server-dialect failed-chain finalization fixtures remain open. |
| Repeated receipt-owner recovery and aggregate terminal ownership | `sqlite_workflow_receipt_owner_survives_repeated_finalization_failure`, `sqlite_failed_chain_recovery_archives_without_reexecution`, and the failed-batch recovery scenario each force at least two recovery finalization failures before a later settlement succeeds. The SQL driver performs a fenced, same-attempt, best-effort repair that restores inherited receipt lineage and adds a bounded delay; direct driver tests cover malformed, stale, and inapplicable repair branches. `mysql_concurrent_batch_receipt_owner` and `postgres_concurrent_batch_receipt_owner` race twelve fail-fast members through separate workers and prove the serialized parent transition produces twelve member receipts, one aggregate-owner receipt, and one terminal failed/cancelled fact pair. `TestSQLStoreBatchAggregateOwnershipMismatchFailsClosed` rejects an aggregate row with missing completion, success-owned cancellation, a missing claimed member, or owner/outcome disagreement with that member's receipt; `TestSQLStoreBatchAggregateIncarnationMismatchFailsClosed` rejects a stale aggregate incarnation. `TestBatchRecoveryRejectsInvalidAggregateReceiptShape` proves cancellation, completion, and live aggregate state must agree before recovery can acknowledge the delivery or produce effects. |

\* `sqlite` is promoted to full restart/durability coverage in the shared suite when queue and worker use the same test-local DSN (see fixture override logic in `integration/all/integration_scenarios_test.go`).

## `UniqueFor` Identity and Scope

Every driver receives one versioned logical identity composed from:

- the effective physical queue name;
- the logical application job type; and
- the canonical serialized logical payload bytes.

Absent payloads, zero-byte payloads, and the exact JSON `null` payload share one canonical absence identity so removing the legacy workflow envelope cannot change a job's key. All other payload bytes remain exact. Generated dispatch, job, chain, and batch IDs are correlation metadata and do not affect duplicate suppression. Retry, delay, timeout, and backoff options are also excluded. This lets two independently constructed workflow envelopes suppress the same logical work without conflating observability IDs with delivery policy.

| Backend | Claim Scope | Failure Boundary |
| --- | --- | --- |
| `null`, `sync`, `workerpool` | One queue runtime instance | Claims live in memory and disappear when that runtime exits. Known pre-acceptance failures release their claim. |
| `mysql`, `postgres`, `sqlite` | All producers sharing the database | The uniqueness claims and queue row commit in one database transaction. |
| `redis` | All producers sharing Redis | TTL must be at least one second. A token-owned Redis claim is released after a definite Asynq physical duplicate. Other enqueue errors are ambiguous because Redis may have committed before the response was lost, so the claim remains until TTL to fail closed against duplicate retries. A producer crash before Asynq acceptance can therefore suppress work until the TTL expires. |
| `nats`, `sqs`, `rabbitmq` | One queue runtime instance | Claims live in memory and disappear when that runtime exits. Failures known to occur before publication release their claim; ambiguous server roundtrip, send, or confirmation failures retain it until TTL to avoid admitting an immediate duplicate. |

Claims use a fixed acquisition-time TTL; successful processing does not shorten the window. Canonical SQL keys deliberately do not perpetuate the old delimiter-based key because doing so would retain its collisions and double high-cardinality lock rows forever. Redis retains Asynq's physical claim alongside the canonical claim for direct-job compatibility. Older public workflow producers embedded volatile IDs in their physical payload, so every public workflow producer must be upgraded in one coordinated cutover. SQL deployments must either quiesce unique dispatches for the largest prior TTL before resuming or explicitly accept a transient mixed-version duplicate window.

## Backend Notes

### Local runtimes (`DriverSync`, `DriverWorkerpool`)

- Sync executes work inline. Workerpool executes with the concurrency configured by `WithWorkers`; when no explicit buffer is supplied, its queue capacity scales with that worker count.
- Once shutdown begins, new public work is rejected. Workflow continuations are admitted only through a runtime-scoped permit held by an active handler; that permit expires when the handler returns and cannot authorize another runtime.
- Accepted delayed work and callback descendants remain part of the drain. A shutdown deadline bounds the current attempt; if it expires, shutdown returns the context error and a later call can continue cleanup.

### Redis (`DriverRedis`)

- Uses Asynq-backed runtime semantics.
- An accepted Asynq task is persisted in Redis, but handlers must tolerate redelivery around worker/process failures.
- Asynq v0.26 archives an exhausted task before consulting its non-failure predicate. New tasks with an explicit retry budget carry one header-marked transport reserve: workers expose the original application budget, explicitly archive terminal application errors, and reuse the reserve for uncommitted workflow or lease-recovery redelivery. Deploy workers before producers; already-queued legacy tasks remain subject to the upstream final-attempt behavior.
- Public workflow dispatch now preserves its zero retry policy instead of allowing Asynq to substitute its default of 25 retries. Applications that intentionally relied on that old fallback must set `.Retry(25)` explicitly. This is a runtime-behavior migration, separate from the worker-first transport-reserve rollout above.
- Shared suite treats custom backoff as unsupported in this runtime path (`supportsBackoff=false`).
- Ordering contract is tested in-suite (`supportsOrderingContract=true`) under the current shared scenario's constrained FIFO assumptions.
- Do not generalize this to multi-worker, retry, or delayed/immediate mixed workloads unless explicitly documented and tested.
- Broker fault scenarios are covered in the shared suite (`supportsBrokerFault=true`).

### Database (`DriverDatabase`: MySQL/Postgres/SQLite)

- Acceptance is a committed queue-row insert. Claimed rows use stale-processing recovery, so application handlers must tolerate redelivery if finalization does not commit.
- Positive process and workflow facts wait for fenced row finalization matching the exact processing claim. Every claim receives an opaque generation ID. When a row remains pending for same-attempt infrastructure redelivery, SQL normally retains any inherited unsettled-generation ID. If the current generation durably writes a workflow transition receipt before later infrastructure work requests that redelivery, the workflow engine marks application state committed and SQL retains the current generation instead. The next claim can therefore match the actual receipt owner rather than an older recovered generation. This signal does not mean queue settlement or observer delivery succeeded. An application retry increments the attempt and clears every prior-generation link. Recovery flags, queue state, and error text are not workflow-transition proof by themselves.
- On the direct built-in SQL workflow-store path, a chain-node or batch-member outcome mutation writes an immutable transition receipt in the same database transaction. The receipt persists `receipt_version` for its ownership format and `event_schema_version` for the shared observer fact contract; both are currently `1`, and the event version is independent from the workflow-envelope protocol version. An unsupported value fails recovery closed with an uncommitted error rather than being collapsed into an absent receipt. Logical receipt proof requires supported versions; a persisted owner with nonempty generation, dispatch, physical job ID, and fingerprint plus a nonnegative owner attempt; matching workflow kind/ID/member/incarnation and logical dispatch; and the same immutable job fingerprint. The current delivery must carry nonempty dispatch and `JobID`, but its attempt is physical provenance and may differ from the owner or be negative. For a chain, the physical `JobID` may also differ from the receipt owner; for a batch, `JobID` is the logical member key and must match. This logical proof suppresses duplicate handler execution. Reconstructing member or aggregate facts is stricter: `RecoveredGenerationID`, current attempt, and physical `JobID` must all equal the receipt owner's tuple. A physical nonowner publishes no reconstructed facts. A failed chain receipt still returns the first persisted terminal cause as permanent, and a failed batch member still returns a generic permanent cause because its original cause is intentionally not persisted. Both failure paths drive SQL toward its `dead` archive without emitting replacement failure/member facts; built-in `FailChain` cannot replace the authoritative first chain cause. A SQL aggregate receipt must own completion; cancellation must own a failed outcome; and when it names the requested logical member, its workflow incarnation, complete physical owner, and outcome must match that member's receipt. Runtime recovery also requires its completion and cancellation flags to agree with live terminal state. Any contradiction fails uncommitted before acknowledgement or effects. `BatchCompleted` is reconstructed only when the exact physical fact owner also owns that validated aggregate transition. Server-dialect parent-row serialization ensures exactly one of concurrent distinct members owns that transition, with real twelve-worker MySQL and PostgreSQL race evidence. Built-in memory receipts are process-local. Receipt-absent legacy rows and custom/decorated/raw stores retain weaker fact and terminal-failure recovery guarantees; state-confirmed predecessor success still suppresses its handler and preserves the live continuation as described below.
- Any recovered predecessor whose validated durable state proves success re-dispatches the immediate successor while the chain is active and `NextIndex` still points to it, without re-running the predecessor. Exact physical fact ownership additionally reconstructs predecessor facts; a missing receipt, a store without receipt capability, or a logically valid receipt with different/legacy generation, different attempt, or different physical `JobID` dispatches only the successor. Progressed and terminal chains are no-ops. This closes definite enqueue rejection and the legacy/custom liveness gap but remains at-least-once: an already-enqueued successor that has not progressed is indistinguishable from a missing one, so duplicate enqueue remains possible. A transition receipt is not a durable continuation intent, settlement outbox, or observer outbox. It does not record callback delivery, `Progress` closures, batch fan-out, or whether a successor enqueue committed. If queue finalization commits and the process exits before deferred observer calls, no queue row remains to trigger recovery. Durable publication and continuation still require the open outbox/intent work.
- After a recovered SQL delivery exhausts its finalization retries without committing new application state, the driver makes one fenced best-effort repair: it returns that exact claim to `pending` on the same attempt, clears its processing timestamp, restores the inherited receipt-owner generation, and delays reclaim by at least the finalization-retry floor. Real SQLite scenarios prove multiple successive success, failed-chain, and failed-batch finalization failures retain lineage until a later delete or archive succeeds. If the repair itself cannot be proven or committed, `settlement_failed` reports the joined error and no stronger lineage guarantee is claimed.
- Startup migrations remain enabled by default. Set `DisableAutoMigrate: true` when an external deployment process owns schema changes; readiness and startup then perform no queue DDL and fail closed unless both `queue_jobs` and `queue_unique_locks` are base-table relations, including PostgreSQL partitioned tables, containing every column used by the current runtime. Views do not satisfy the managed-runtime contract. A failed managed-schema check does not consume startup, so the same runtime can become ready and start after deployment tooling provisions the complete schema. This is a presence gate, not certification of write permissions, exact SQL types, constraints, or performance indexes; externally managed deployments must still apply the canonical dialect schema. An automatic migration failure is also retryable after its lock, permission, or connectivity problem is corrected.
- Processing fencing adds one nullable `processing_token` column, so existing rows and producer-only old binaries remain readable. When migrations are externally managed, add that column before starting new workers. Do not overlap old and new SQL workers during rollout: old workers settle by row ID and cannot honor the new claim-generation fence.
- `queue.NewSQLStore` preserves the established migration-on-first-use behavior and additively creates `bus_workflow_transition_receipts`, including non-null `receipt_version` and `event_schema_version` columns. Real SQLite, MySQL, and PostgreSQL tests cover fresh auto-schema plus receipt-backed terminal-chain recovery. `SQLStoreConfig.AutoMigrate` remains in the compatibility shape, but its false zero value does not opt out. A wholly fresh MySQL schema uses `VARBINARY(255)` workflow/member and receipt identities. When established state tables exist but the receipt table is missing, startup validates their byte-exact `VARBINARY` keys and derives one shared receipt width: `workflow_id` is the larger effective chain-or-batch ID capacity, and `member_id` is the larger chain-node-or-batch-job capacity. `TestWorkflowStoreIntegration_MySQLAutoMigratesMissingReceiptAtLegacyWidths` proves this preserves a live 512-byte legacy schema and identities above the fresh defaults. Automatic startup never alters an existing receipt table; its capacities instead participate in the connected-schema intersection. An existing incompatible receipt table therefore requires a quiescent managed migration and a fresh store instance. If the derived composite primary key exceeds the MySQL server's indexed-key budget, receipt creation fails with both derived widths and schema-first guidance rather than narrowing or altering established tables. Operators must then quiesce workers, audit live identities, and precreate a compatible indexed receipt schema; if the server cannot represent the required capacities, identity limits and existing data need an explicit migration before rollout. Those exceptional cases are persisted-schema, runtime-behavior, and operational concerns, not source/API, configuration-file, wire, or minimum-Go-version changes. Externally managed workflow schemas must precreate every dialect-correct workflow table and use `queue.NewSQLStoreWithManagedSchema` before new workflow workers start. Rollback should quiesce new workers first and leave the additive receipt table in place; old binaries ignore it, while dropping it destroys recovery provenance and old pruning code may otherwise leave receipt rows behind. Managed-schema migration/rollback, real cross-dialect pruning, and physical commit/readback ambiguity when the database or context is unavailable remain open.
- The additive opt-out does not yet replace a versioned rollout policy. Operators that keep automatic migration enabled must grant the required DDL permissions and coordinate concurrent startup, especially for the uniqueness expiry index; MySQL and PostgreSQL concurrency/permission evidence remains open.
- Supports retry/backoff, poison retry, dispatch context cancellation, and deterministic duplicate prevention in the shared suite.
- Broker fault injection scenarios are not enabled in the shared suite for DB backends.
- DB backends rely on stale-`processing` recovery behavior for crash recovery (`process_recovered` event visibility is important operationally).
- Ordering is not currently claimed in the shared suite for MySQL/Postgres. SQLite ordering is only claimed in the suite under test-local conditions (see matrix note).

### NATS (`DriverNATS`)

- The current implementation uses Core NATS publish/subscribe, not JetStream. It is an ephemeral broker adapter: there is no durable consumer acknowledgement, retained work queue, or crash recovery boundary.
- Plain subscriptions are broadcast semantics, not competing-consumer queue semantics: every worker subscription on the same subject, whether in one process or several, can receive a copy.
- Worker startup reports success only after the server has observed the subscription, and failed startup can be retried. Shutdown waits for callbacks and delayed work already admitted to that worker before closing its producer connection.
- Initial and replacement publishes flush through a server roundtrip before reporting success. This proves only that the Core NATS server observed the ephemeral publish; it is not a durable queue acknowledgement.
- Retry is republish-based. Shutdown drains the worker's subscription before admitted handlers finish replacement publication; without another subscriber, Core NATS can accept and then discard that replacement. A process or connection failure can likewise lose the original or replacement message, so this backend does not currently conform to a durable committed-retry contract.
- The shared suite exercises core dispatch/processing and backoff/poison behavior only while the fixture remains available.
- Shared suite does not claim restart durability guarantees for delayed/retried work (`supportsRestart=false`, `supportsRestartDelayedDurability=false`).
- Deterministic no-duplicate and ordering guarantees are not claimed in the suite.
- Users should treat ordering as non-guaranteed unless a stronger constrained contract is explicitly added and tested.

### SQS (`DriverSQS`)

- Initial and replacement acceptance require a successful SQS `SendMessage` response with a non-empty service-generated message ID. SQS delivery is redeliverable through visibility timeout, so handlers must be idempotent.
- Retry republishes before deleting the original. Positive process and workflow facts wait for `DeleteMessage`; missing receipts or delete failures emit `settlement_failed` and leave the original eligible for redelivery, so duplicate handling remains mandatory.
- Supports restart recovery in the shared suite, but not delayed/retry restart durability guarantees (`supportsRestartDelayedDurability=false`).
- Broker fault scenarios are not deterministically exercised in the shared suite.
- Ordering guarantees are not claimed.
- Workers do not yet extend message visibility while a handler runs, and each receive can return several messages that one worker processes sequentially. Configure visibility for the worst-case interval from receive through completion of the last message in that batch, and expect duplicate delivery if processing exceeds it; M4-03 tracks heartbeat/extension support.
- Local integration validation uses LocalStack.

### RabbitMQ (`DriverRabbitMQ`)

- Initial and replacement persistent publishes require a positive publisher confirmation before dispatch succeeds or the original delivery is acknowledged.
- Worker retry publishes before acknowledging the original. Positive process and workflow facts wait for Ack. A negative confirmation permits safe claim compensation; a missing, canceled, or failed confirmation is treated as ambiguous and does not trigger reconnect-republish or uniqueness release. Ack/Nack failures emit `settlement_failed` because the original may redeliver.
- Supports restart recovery and delayed/retry restart durability in the shared suite.
- A worker does not yet reconnect after its delivery channel closes; reconstruct the queue runtime after connection loss. Dial retries and AMQP channel/connection closure are not fully context-aware and can overrun a caller's lifecycle deadline.
- Broker fault scenarios are not deterministically exercised in the shared suite.
- Ordering guarantees are not claimed.

## Ordering Guarantee Rules (Current Public Position)

Until the shared ordering contract is split into condition-specific scenarios, the safe public posture is:

- Ordering is **not guaranteed by default** across backends.
- Any FIFO behavior observed under a specific backend/test setup should be treated as a constrained implementation detail unless documented here with explicit preconditions.
- Retries, delays, and multi-worker concurrency can reorder execution and should be assumed to do so unless a backend-specific guarantee says otherwise.

## Production Guidance Notes

- Treat this matrix as the contract for what the shared integration suite validates today.
- Do not infer cross-process `UniqueFor` behavior from an instance-scoped backend. Use the scope and failure boundaries documented above.
- If you change a capability flag in `integration/all/integration_scenarios_test.go`, update this document in the same PR.
- If you want to claim a stronger backend guarantee publicly, add or unskip the corresponding shared scenario first.

## GA Completion Criteria for This Document

Before GA, ensure this document also includes:

- backend-specific configuration guidance links (timeouts, concurrency, retries)
- any known caveats/limits by backend version
- links to the latest passing shared integration evidence and soak evidence
